fn solve(){
	println!("hello world!");
}

fn main(){
	let t:i32=read1();
	readv::<i32>();
	readt!(u32);
	for i in 1..t+1{
		print!("Case #{}: ",i);
		io::stdout().flush().unwrap();
		println!("hello world!");
		solve()
	}
}
use psrs::IO::*;
use std::io;
use std::io::Write;