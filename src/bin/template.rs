fn solve(io:&mut psrs::io::IO){
}

fn main(){
	let mut io = psrs::io::IO::new();
	// let t:usize=io.pop();
	// for i in 1..t+1
	// {io.push("Case #").push(i).push(": ");solve(&mut io)}
	{solve(&mut io);}
}