fn solve(io:&mut psrs::io::BulkIO){
}

fn main(){
	let mut io = psrs::io::BulkIO::new();
	// let t:usize=io.pop();
	// for _i in 1..t+1
	// {io.push("Case #").push(_i).push(": ").flush();solve(&mut io)}
	{solve(&mut io);}
}